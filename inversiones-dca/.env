# Example environment configuration
# Database connection string (PostgreSQL). Replace with Supabase or local connection.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/inversiones_dca?schema=public"

# NextAuth configuration
NEXTAUTH_SECRET="your-random-secret"
NEXTAUTH_URL="http://localhost:3000"

# Email provider configuration for NextAuth magic link
EMAIL_SERVER=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=username
EMAIL_PASSWORD=password
EMAIL_FROM="Inversiones DCA <no-reply@example.com>"

# Rate limiting configuration (Upstash or similar)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Cron job secret (used by scheduled functions)
CRON_SECRET="your-cron-secret"